# MaxGPT-Ultra dependencies (grows as milestones land)
torch>=2.4
pyyaml
tokenizers

# arriving with later milestones:
#   numpy            (data shards / arrays)
#   datasets         (FineWeb-Edu etc. download + streaming)
#   bitsandbytes     (8-bit AdamW so the 1B's optimizer states fit in 12GB)
#   fastapi, uvicorn, websockets   (the training dashboard)
